node_modules
dist
dist-ssr
*.local
.DS_Store
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal
server/data/*.db-shm
server/data/*.bak*

# Source images dropped in the repo root (logos etc.) — keep the built asset in public/, not the source
WhatsApp Image*
.vscode/*
!.vscode/extensions.json
npm-debug.log*

# scratch notebook screenshots
Pasted image*.png

# Off-server snapshots of the live production DB (real business data — never commit)
backups/
