node_modules
dist
dist-ssr
*.local
.DS_Store
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal
server/data/*.db-shm
server/data/*.bak*
server/data/uploads/

# Source images/docs dropped in the repo root — keep built assets in public/, not the source
WhatsApp Image*
QEG*.pdf
.vscode/*
!.vscode/extensions.json
npm-debug.log*

# scratch notebook screenshots
Pasted image*.png

# Off-server snapshots of the live production DB (real business data — never commit)
backups/

# Playwright MCP session artifacts
.playwright-mcp/

# Root-level Excel sources (fleet ledger + employee personal data — never push)
/*.xlsx
